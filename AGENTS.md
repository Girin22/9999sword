# AGENTS.md — 9999검키우기 (fableknights)

이 파일은 Codex(및 모든 코딩 에이전트)가 이 레포에서 작업할 때의 지침이다. **먼저 읽고, 의심되면 `docs/GDD.md`가 정답이다.**

## 0. 한 줄 요약

세로 화면 웹 게임. 병사들은 알아서 싸우고, 유저는 대장간에서 **검을 강화**하고 그 검을 **언제·누구(병종)에게 보급할지**만 정한다. 검을 든 병사가 쓰러지면 그 검은 쇠해서 +1 고철이 된다. 출품용 프로토타입 — **게임 루프가 플레이 가능한 것**이 목표다.

## 1. 반드시 지킬 제약

- **플랫폼**: 웹, 정적 배포(Vercel/GitHub Pages). 서버·계정·네트워크 호출 **없음**. 런타임 LLM 호출 **없음**.
- **화면**: **세로 고정** 기준 해상도 1080×1920 (9:16). 가로 모드 없음. PC에서는 가운데 세로 캔버스 + 양옆 여백.
- **조작**: 클릭/탭만. 키보드·드래그 없음.
- **인게임 텍스트 금지**: 전투 화면에 대사·말풍선·내레이션을 띄우지 않는다. HUD는 수치·바·아이콘만. 텍스트는 아웃게임(로비·결산)에서만.
- **마나 없음.** 질서의 비용은 시간(쿨타임·중첩), 혼돈의 비용은 특성마다 정해진 대가.
- **저장**: `localStorage` 키 하나에 JSON. 스테이지 도중 이탈 = 포기. 햄버거 메뉴에 "게임 초기화"(경고 + 재확인).
- **배속**: ×1 / ×2 토글, 우상단 HUD. 저장하지 않고 스테이지마다 ×1로 시작.
- **수치는 코드에 하드코딩하지 않는다.** 전부 `data/*.json`에서 읽는다. 밸런스 조정은 JSON만 바꿔서 되게.

## 2. 기술 스택 (확정)

- **Phaser 3 + Vite + TypeScript**. 패키지 매니저는 `npm`.
- 씬: `Boot` → `Title` → `Lobby` → `Formation` → `Battle` → `Result`. `Battle` 안에 HUD는 별도 Scene(`BattleHUD`)으로 겹쳐 띄운다.
- 상태 관리: 전투 로직은 Phaser 오브젝트와 분리된 **순수 TS 시뮬레이션 모듈**(`src/sim/`)로 작성한다. 렌더는 `src/scenes/`가 sim 상태를 읽어서 그린다. 이렇게 해야 헤드리스 테스트(vitest)로 루프를 검증할 수 있다.
- 테스트: `vitest`. `src/sim/`은 반드시 단위 테스트를 둔다(강화 확률·장인의 기운·기절/쇠한 검·보급·성벽·드롭).
- 린트/포맷: `eslint` + `prettier` 기본 설정. 경고 0으로 유지.

## 3. 디렉터리 구조 (이대로 만든다)

```
/
├─ AGENTS.md              ← 이 파일
├─ docs/
│  ├─ GDD.md              ← 게임 규칙 스펙 (정답)
│  ├─ ROADMAP.md          ← 개발 로드맵 (마일스톤·완료 조건)
│  ├─ MANIFEST.md         ← data/*.json 스키마와 의미
│  ├─ UI_LAYOUT.md        ← UI 레이아웃 (유저 목업 확정 좌표)
│  ├─ ART_STYLE.md        ← 아트 톤·캐릭터 리그·전투 연출·에셋 분담
│  ├─ reference/          ← 레퍼런스 이미지 (레이아웃)
│  (doodle_ref/ 루트 폴더 = 아트 레퍼런스 5장)
│  └─ gdd-full.html       ← 원본 기획서 (참고용, 표 편집 스크립트 포함)
├─ data/                  ← 모든 게임 수치 (JSON, 코드에서 import)
│  ├─ constants.json
│  ├─ units.json
│  ├─ swords.json
│  ├─ traits.json
│  ├─ monsters.json
│  ├─ stages.json
│  ├─ growth.json
│  └─ options.json       ← 부가 옵션 줄(치명·버프·디버프·방어 무시), 줄 수 상한, 옵션/모루 레시피
├─ public/assets/         ← 이미지·사운드 (없으면 플레이스홀더 도형으로 그린다)
├─ src/
│  ├─ main.ts
│  ├─ config.ts           ← 해상도·스케일 모드
│  ├─ data/               ← JSON 로더 + 타입
│  ├─ sim/                ← 순수 로직 (Phaser 의존 없음)
│  │  ├─ forge.ts         ← 강화·장인의 기운·형상 굴림·특성 부여
│  │  ├─ supply.ts        ← 보급·나 강림
│  │  ├─ combat.ts        ← 유닛/몬스터 틱, 타겟팅, 피해 공식
│  │  ├─ barracks.ts      ← 회복(침상)·기절·쇠한 검
│  │  ├─ mine.ts          ← 광산 생산
│  │  ├─ wave.ts          ← 웨이브·거대 몬스터·성벽
│  │  ├─ traits/          ← 특성 효과 구현 (특성 id별 파일)
│  │  └─ save.ts          ← localStorage
│  ├─ scenes/             ← Boot, Title, Lobby, Formation, Battle, BattleHUD, Result
│  └─ ui/                 ← 버튼·게이지·패널 공용 컴포넌트
└─ tests/                 ← vitest
```

## 4. 작업 순서

`docs/ROADMAP.md`의 마일스톤 순서를 따른다. **M3(인게임 루프 플레이 가능)까지가 1순위**다. 그 전에는 로비·성장·결산을 꾸미지 않는다. 각 마일스톤의 "완료 조건"을 전부 만족해야 다음으로 간다.

## 5. 에셋이 없을 때

`public/assets/`에 파일이 없으면 **도형 플레이스홀더**로 그린다: 병사 = 둥근 사각형(body) + 양옆 작은 원 2개(hand), 검 = hand에 붙는 길쭉한 사각형(+N에 따라 길이·두께, 종류에 따라 색), 몬스터 = 같은 도형에 테두리 색만 다르게, 거대 몬스터 = 4배 크기. **리그 구조(body + hand 2 + 소켓)와 전투 트윈(부딪침·구르기·깃발 펄스)은 플레이스홀더 단계부터 구현한다** — 에셋 교체는 텍스처만 바뀐다. 텍스트 라벨로 이름/+N을 붙여도 된다(프로토타입 한정 — 최종 HUD 원칙과 별개). 에셋이 들어오면 `src/render/sprites.ts` 한 곳만 바꿔서 교체되도록 렌더를 분리한다.

## 6. 하지 말 것

- 기획서에 없는 시스템을 추가하지 않는다(장비 슬롯, 스킬 트리, 뽑기 등).
- 유닛에 검과 무관한 공격력/공속/이속 스탯을 두지 않는다. 유닛 스탯은 **체력·방어력·식량**뿐. 공격력·속도는 검에서 온다.
- 인게임 건물 레벨업, 성벽 수리, 재료의 다른 용도를 만들지 않는다. 재료는 강화에만 쓴다.
- 질서/혼돈 롤이 게임 상태(거대 몬스터 접근 등)를 참조하게 만들지 않는다.
- 카운터(형상 5회, 장인의 기운 10회)를 +N 기준으로 세지 않는다. **강화 버튼 클릭 수** 기준이다.

## 7. 명령

```
npm install
npm run dev        # Vite dev server
npm run build      # 정적 빌드 → dist/
npm run test       # vitest
npm run lint
```

## 8. 커밋 규칙

마일스톤 단위로 커밋. 메시지는 `M2: 강화·보급 루프` 처럼 마일스톤 번호를 앞에 붙인다. `data/*.json` 수치를 바꿨으면 커밋 메시지에 어떤 값을 왜 바꿨는지 한 줄 적는다.
